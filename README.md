# Dockerize Dotnet App with Postgres and Traefik
A lightweight, production-ready CRUD web application built using ASP.NET Core (.NET 10), PostgreSQL , and Traefik as a reverse proxy — all containerized with Docker and Docker Compose. 
